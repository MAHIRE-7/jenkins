eksctl create cluster --name wordtopdf --nodegroup-name wordtopdf-nodes --nodes 2 --nodes-min 2 --nodes-max 3 --region ap-south-1 --managed --node-type t3.medium  


## SonarQube Server Installation

docker run -itd --name SonarQube-Server -p 9000:9000 sonarqube:lts-community


## SonarQube scanner
sudo apt update
sudo apt install curl -y
cd /opt
sudo curl -fL -o sonar-scanner.zip \
https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-5.0.1.3006-linux.zip
sudo unzip sonar-scanner.zip
sudo mv sonar-scanner-5.0.1.3006-linux sonar-scanner
sudo ln -s /opt/sonar-scanner/bin/sonar-scanner /usr/bin/sonar-scanner

## OWASP
Jenkins UI →
Manage Jenkins → Plugins → Available

Search:

OWASP Dependency-Check -> install & restart

Manage Jenkins → Tools -- Scroll down → Dependency-Check

Add:
Name: owasp
✔ Install automatically


## ARGOCD
kubectl create namespace argocd
kubectl apply -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml

